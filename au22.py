import tkinter as tk

root = tk.Tk()
root.title("auusef")

# Create a label
label = tk.Label(root, text="EROR")

# Place the label at specific coordinates
label.place(x=50, y=50)

root.mainloop()